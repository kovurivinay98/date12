- name: Installing multiple packages
  hosts: node
  become: true
  tasks:
    - name: Installing packages
      ansible.builtin.package:
        name: "{{ item }}" 
        state: present
  loop:
  - mysql
  - zip
  - nginx